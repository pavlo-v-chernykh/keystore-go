modules-download-mode: readonly

linters:
  enable-all: true
  disable:
    - gochecknoglobals
    - funlen
    - goerr113
    - gofumpt
    - exhaustivestruct
    - gomoddirectives
    - scopelint
    - makezero
    - golint
    - interfacer
    - maligned
    - varnamelen
    - exhaustruct

linters-settings:
  cyclop:
    max-complexity: 15


issues:
  exclude:
    - import '.*' is not allowed from list 'Main'
  exclude-rules:
    - path: _test\.go
      linters:
        - testpackage
        - paralleltest
        - maligned
        - dupl
    - linters:
        - gosec
      text: "G401: "
    - linters:
        - gosec
      text: "G505: "
